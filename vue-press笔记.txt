文件名是为了路由生成，文章内容才是具体的文章信息



一个父div有滚动条，其子div相对于父div的绝对定位是根据其内高，即要算上滚动条的偏移量

开发依赖
   不会将其打包到最终产出的js中
   但是在build时，还是会用到这些依赖进行打包

生产依赖
  会将其打包到最终产出的js中
  
  
  css选择器
     伪元素选择器后面还可以接着用选择器选择子元素
	 
	 
	 
a标签：
   href三种情况：
       有http://，直接识别为请求
	   无http：//,就仅仅是url跳转到网站+href，页面不会发出请求，单页面就是通过此来实现的，监听histroy的api
	   #，直接让该锚点在屏幕的可视区内